# _config
linux env config

## 使用

### nvim配置
```shell
ln -s `pwd`/.config/nvim $HOME/.config/nvim
```

### bash配置
```shell
ln -s `pwd`/.bashrc $HOME/.bashrc
```

### tmux配置
```shell
ln -s `pwd`/.tmux.conf $HOME/.tmux.conf
```

